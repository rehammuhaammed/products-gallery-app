/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', //  enables toggling dark mode with a class
    content: [
        "./src//*.{html,ts}"
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}